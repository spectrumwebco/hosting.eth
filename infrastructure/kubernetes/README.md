# Kubernetes Configuration

This directory contains Kubernetes manifests for deploying the hosting.eth platform.

## Structure

- `base/`: Base Kubernetes configurations
- `overlays/`: Environment-specific overlays (production, staging, development)
